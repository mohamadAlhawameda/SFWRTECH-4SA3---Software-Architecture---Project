# ExpenseTracker Lite – Milestone 2 (DB Connectivity)

This demo proves cloud DB connectivity by:
1) Connecting to PostgreSQL (Supabase/Neon)
2) Creating a simple `expenses` table
3) Inserting a sample row
4) Selecting it back

## Setup
1. Create a `.env` from `.env.example` and fill `DATABASE_URL`:
